{{ $email := env "EMAIL" }}
{{ if not $email }}
{{ $email = promptStringOnce . "email" "Email address" }}
{{ end }}

{{ $hasGUI := env "GUI" }}
{{ if not $hasGUI }}
{{ if eq .chezmoi.os "darwin" }}
{{ $hasGUI = true }}
{{ else }}
{{- $hasGUI = promptBoolOnce . "hasGUI" "Does this machine have a GUI [y/n]" -}}
{{ end }}
{{ end }}

{{ $isWork := "" }}
{{ if eq .chezmoi.fqdnHostname "*.infra.franta.us" }}
{{ $isWork = false }}
{{ else if eq .chezmoi.fqdnHostname "*.osc.edu" }}
{{ $isWork = true }}
{{ else }}
{{- $isWork = promptBoolOnce . "isWork" "Is this machine used for work [y/n]"  -}}
{{ end }}

encryption = "gpg"
[gpg]
    recipient = "0xB55027F11D7D24C8"
[data]
    email = {{ $email | quote }}
    hasGUI = {{ $hasGUI | quote }}
    isWork = {{ $isWork | quote }}
[git]
    autoCommit = true
[diff]
    command = "nvim"
    args = ["-d", "{{`{{ .Destination }}`}}", "{{`{{ .Target }}`}}"]

[merge]
    command = "nvim"
    args = ["-d", "{{`{{ .Destination }}`}}", "{{`{{ .Source }}", "{{ .Target }}`}}"]
